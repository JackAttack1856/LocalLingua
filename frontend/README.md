# LocalLingua Frontend

## Dev
- Install: `pnpm install`
- Run: `pnpm dev`

Env:
- `VITE_API_BASE_URL` (defaults to `http://localhost:8000`)

If `pnpm install` warns about ignored build scripts (e.g. `esbuild`) and Vite can’t start, run `pnpm approve-builds`.
